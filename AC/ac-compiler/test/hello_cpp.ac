AC->C++ *backend*
<mainloop>
     <LOGIC>
          Term.display $Starting up...$
          list = [$apple, banana, cherry$]
          FOR item in list
               Term.display(item)
          Make factorial func(arg)
               IF arg = 0
                    return 1
               ELSEIF arg = 1
                    return 1
               OTHER
                    result = fn arg*(factorial(fn arg-1))
                    return result
          result = factorial(5)
          Term.display(result)
          /kill
     <LOGIC>
<mainloop>
