// Generated by AC Compiler (AC->JS)
"use strict";

function main() {
    while (true) {
        console.log(`Before foreign code`);
        // Preposterous: Foreign code is too Foreign (if this breaks, that's on you)
        const nums = [1, 2, 3, 4, 5];
        const sum = nums.reduce((a, b) => a + b, 0);
        console.log("Foreign JS: sum =", sum);
        console.log(`After foreign code`);
        if (typeof process !== 'undefined') process.exit(0); else throw new Error('__ac_exit__');
    }
}

main();
